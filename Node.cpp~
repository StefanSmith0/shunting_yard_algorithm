#ifndef NODE_H
#define NODE_H
#include <iostream>

using namespace std;

class Node {
public:
  Node(int);
  ~Node();
  int getValue();
  void setValue(int);
  Node* getNext();
  void setNext(Node*);
private:
  int value;
  Node* nextNode;
};
#endif
